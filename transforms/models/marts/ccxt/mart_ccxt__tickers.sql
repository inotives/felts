select
    exchange_id,
    symbol,
    base_asset,
    quote_asset,
    timestamp_ms,
    provider_datetime,
    bid,
    ask,
    last_price,
    open_price,
    high_price,
    low_price,
    close_price,
    base_volume,
    quote_volume,
    vwap,
    percentage,
    observed_at,
    extracted_at,
    loaded_at,
    raw_record_id,
    source_record_id,
    batch_id,
    raw_payload
from {{ ref('stg_ccxt__tickers') }}
