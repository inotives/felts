# Handoff: Phase 08 Source Scaffolding

## Current State

- Repository: `/Users/inotives/workspaces/felts`
- Branch: `main`
- HEAD: `0f272a9 Document phase 08 source scaffolding`
- `main` is clean and synchronized with `origin/main`.
- Phase 08 planning and grilling are complete.
- The Phase 08 documentation commit has been pushed.
- Phase 08 implementation has not started.

The finalized implementation requirements and acceptance criteria are in:

- `docs/phases/phase_08_source_scaffolding.md`
- `docs/implementation_phases.md`

Do not repeat the design interview unless implementation exposes a genuine gap.

## Next Session Objective

Implement Phase 08 on a new feature branch from the latest `main`.

The main deliverable is `scripts/scaffold.py`, supporting:

```text
scripts/scaffold.py source <source> --base-url <url>
scripts/scaffold.py entity <source> <entity> ...
```

Use the exact behavior, constraints, generated artifacts, managed registry blocks,
validation commands, and follow-up checklist defined in the Phase 08 document.

## Recommended Execution Order

1. Read `AGENTS.md` and `docs/phases/phase_08_source_scaffolding.md`.
2. Confirm `main` is current, then create a feature branch such as
   `phase-08-source-scaffolding`.
3. Use CodeGraph before reading or changing the CLI, orchestrator, CoinGecko source,
   tests, and dbt registration paths.
4. Implement collision-safe, transactional source and entity scaffolding.
5. Add scaffold-managed registry blocks only where required.
6. Generate and complete the Alpha Vantage acceptance Source described in the phase doc.
7. Run focused scaffold tests, Ruff, `dbt parse`, the relevant project test suite, and
   live Alpha Vantage acceptance tests within the free-tier request budget.
8. Verify existing CoinGecko and CSV behavior is unchanged.
9. Commit and push the feature branch, then provide PR notes.

## Operational Context

- Production deployment documentation: `docs/linux_production_deployment.md`
- Linux Mint deployment currently runs Postgres in Docker and Prefect server/worker
  through systemd.
- Environment files belong under `settings/` as `.env.local`, `.env.dev`, and
  `.env.prod`; only example files are committed.
- A local Alpha Vantage key is available in an ignored local environment file. Never
  print, log, or commit its value.
- Alpha Vantage free-tier acceptance calls must be spaced by at least two seconds and
  kept within the documented daily request allowance.
- A previously exposed CoinGecko credential must be treated as compromised and rotated;
  no credential value is included here.

## Suggested Skills

- `codebase-design`: use while locating the narrowest integration points for the
  scaffolder and managed registry blocks.
- `diagnosing-bugs`: use if generated-code validation, Prefect registration, dbt parsing,
  or live provider behavior fails unexpectedly.
- `review`: use before the PR to compare the implementation against Phase 08 and project
  standards.
- `handoff`: use again if implementation cannot be completed in the next session.

## Completion Boundary

Phase 08 is complete only after the implementation branch passes the phase acceptance
criteria, live Alpha Vantage testing is documented, the PR is merged, `main` is pulled,
and `docs/phases/phase_08_source_scaffolding.md` is moved to `docs/_archived/`.
